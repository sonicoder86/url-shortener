# Skeleton

## Development

Start the server (Fastify) locally, it'll restart on file changes automatically.

```shell
cp .env.example .env
npm run dev
```

Run all the tests (Jest).

```shell
npm test
```
